`EdgeCases.fsd` is used by `EdgeCaseTests` to test code generation scenarios that are too obscure for `ConformanceApi`. The build script also uses `EdgeCases.fsd` to generate the `EdgeCases` project.
